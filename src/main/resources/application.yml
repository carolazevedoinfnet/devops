spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://devops-db-server.mysql.database.azure.com/devops?useTimezone=true&serverTimezone=UTC
    username: caag
    password: Carolina76
  jpa:
    hibernate:
      ddl-auto: update
    #properties:
      #hibernate:
        #dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
    open-in-view: false
